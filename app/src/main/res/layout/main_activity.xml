<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".ui.MainActivity">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="center"
        android:orientation="vertical">

        <Button
            android:id="@+id/btn_demo01"
            style="@style/MainActivityButton"
            android:onClick="onBtnClick"
            android:text="分别用ImageView,SurfaceView,自定义View绘制图片" />

        <Button
            android:id="@+id/btn_demo02"
            style="@style/MainActivityButton"
            android:onClick="onBtnClick"
            android:text="AudioRecord和AudioTrack采集pcm读写wav文件" />

        <Button
            android:id="@+id/btn_demo03"
            style="@style/MainActivityButton"
            android:onClick="onBtnClick"
            android:text="Camera2" />
    </LinearLayout>
</ScrollView>